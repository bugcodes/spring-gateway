server:
  port: 9002
#  servlet:
#    context-path: /auth2
spring:
  application:
    name: auth2
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    database: 0
    port: 6379
    host: 127.0.0.1
    password:
management:
  endpoints:
    web:
      exposure:
        include: "*"
